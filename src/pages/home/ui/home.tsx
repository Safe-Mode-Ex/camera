import { FilledButton } from '../../../shared/ui/button';
import { Logo } from '../../../shared/ui/logo';

function Home() {
  return (
    <main>
      <FilledButton>Купить</FilledButton>
      <Logo className="header__logo" />
    </main>
  );
}

export default Home;
